
<h1><%= @task.tittle %></h1>
<%= link_to "Edit this task", tasks_edit_path %>
<h3><%= @task.details %></h3>
 <p>[<%= @task.completed ? "X" : " " %>] <%= @task.completed ? "This task is completed" : "This task is not completed yet" %></p>

<%= link_to "back to list", tasks_path %>
